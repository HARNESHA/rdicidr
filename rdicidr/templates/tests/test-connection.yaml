apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rdicidr.fullname" . }}-test-connection"
  labels:
    {{- include "rdicidr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rdicidr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
